# Core runtime
pypdf
fastapi
uvicorn[standard]
openai
tqdm
python-dotenv
pydantic
requests
python-multipart

# Numerical / ML
numpy>=1.21
sentence-transformers>=2.2.2
sentencepiece

# Vector search (optional on Windows)
faiss-cpu ; platform_system != "Windows"

# Notes:
# - PyPDF2 is deprecated; replaced with pypdf.
# - On Windows, `faiss-cpu` can be tricky to install; the project will fall back to a NumPy-based search if faiss is not installed.
# - If you don't need local embeddings (e.g., you only use OpenAI embeddings), you may remove `sentence-transformers` and `sentencepiece`.
